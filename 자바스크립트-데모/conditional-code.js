const myName = 'MinJi';

if (myName === 'MinJi') {
    console.log('Helo!');
}

let isLoggedIn = true;

if (!isLoggedIn) {
    console.log('User is  NOT logged in!');
}

const enteredUserName = ' '; // 0 빈문자열이나 0은 거짓

if (enteredUserName) {
    console.log('Input is valid!');
}